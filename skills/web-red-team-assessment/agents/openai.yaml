interface:
  display_name: "Web Red Team Assessment"
  short_description: "Local-only destructive pentest with attack lenses"
  default_prompt: "Use $web-red-team-assessment to set up a sibling worktree, copy .env.local as needed, run the local app, extract seed-derived credentials, split attack-lens subagents when appropriate, and report evidence-backed findings plus cleanup notes."
